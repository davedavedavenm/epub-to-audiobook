# Data directories (contain user files / runtime state — not source)
data/

# SSH keys and secrets (sensitive)
ssh-keys/
.secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Agent / tool state
.claude/
.gemini/

# Temp scripts
_tmp_*

# Playwright CLI logs/snapshots
.playwright-cli/

# local runtime artifacts
jobs.db
*.epub
*_tts.epub
docker-compose.override.yml.bak*
piper/config/
piper/voices/

# Local working artifacts (kaggle kernels, rendered audio) — not repo material
output/
scratch/
evaluations/cpu-engines/output/
evaluations/new-engines/output/
